export enum AppNames {
  NOTES = 'Notes',
  FILES = 'Files',
  BROWSER = 'Browser',
  CAMERA = 'Camera',
  GALLERY = 'Gallery',
  NEWS = 'News',
}

export enum Size {
  SMALL = 'small',
  LARGE = 'large',
}
