export enum AppNames {
  NOTES = 'Notes',
  FILES = 'Files',
  BROWSER = 'Browser',
  CAMERA = 'Camera',
  GALLERY = 'Gallery',
  NEWS = 'News',
}

export enum Size {
  SMALL = 'small',
  LARGE = 'large',
}

export enum SortableFields {
  NAME = 'Name',
  LAST_MODIFIED = 'Last Modified',
  CREATED = 'Created',
}

export enum Routes {
  SPLASH_SCREEN = '/',
  LOGIN = '/login',
  DESKTOP = '/desktop',
}
